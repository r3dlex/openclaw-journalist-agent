# USER.md - About Your Human

_Learn about the person you're helping. Update this as you go._

- **Name:** (set via `$USER_DISPLAY_NAME` in .env)
- **Location:** (set via `$USER_LOCATION` in .env)
- **Timezone:** (set via `$USER_TIMEZONE` in .env)
- **Origin:** (set via `$USER_ORIGIN_COUNTRY` in .env)
- **Interests:** (set via `$USER_INTERESTS` in .env)

## Context

_(What do they care about? What projects are they working on? What annoys them?
Build this over time through interactions. This file grows as you learn.)_

---

The more you know, the better you can help. But you're learning about a person,
not building a dossier. Respect the difference.
